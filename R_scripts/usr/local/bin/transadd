#!/bin/sh

# Mimeapp script for adding torrent to transmission-daemon, but will also start the daemon first if not running
# Source: https://github.com/LukeSmithxyz/voidrice/blob/master/.local/bin/transadd
# transmission-daemon sometimes failes to take remote requests in its first moments.

pgrep -x transmission-da >/dev/null || (transmission-daemon && notify-send "Starting transmission daemon..." && sleep 3)

transmission-remote -a "$@" && notify-send "🔽 Torrent added."
