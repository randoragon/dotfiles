-- https://clangd.llvm.org/config
-- Not sure if it's even possible to specify server settings here,
-- clangd conventionally relies on configuration files per project,
-- and it makes sense due to the varied nature of C/C++ environments.
return {}
