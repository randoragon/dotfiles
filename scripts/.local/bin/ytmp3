#!/bin/sh

# youtube-dl wrapper for downloading single mp3 files in best quality.

youtube-dl                                   \
    -x                                       \
    -o '~/Downloads/Music/%(title)s.%(ext)s' \
    --audio-quality 0                        \
    --audio-format mp3                       \
    --no-playlist                            \
    "$@"
